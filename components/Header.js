import React from "react"

export default function Header(props) {
    return (
        <header className="header">
            <i className="ri-flight-takeoff-line"></i>
            <span className="header--intro">my travel journal.</span>
        </header>
    )
}